# LEVEL 31

## Subir commit
Crear archivo
```bash
nano archivo.txt
# Agregar contenidos
```

Agregar archivo al git local
```bash
git add ./archivo.txt

# Agregar un mensaje al commit
git commit -m "Mensaje" 
```

Subir el repo al remoto
```bash
git push origin master
```

